export type BotDifficulty='Easy'|'Medium'|'Hard'|'Extreme';export type Bot={id:string;name:string;difficulty:BotDifficulty;isBot:true};const weights:Record<BotDifficulty,number>={Easy:.25,Medium:.5,Hard:.75,Extreme:.94};export function createBot(n:number,difficulty:BotDifficulty):Bot{return{id:`bot-${Date.now()}-${n}`,name:`Bot ${n}`,difficulty,isBot:true}}export function gunDecision(bot:Bot,targetCount:number){const skill=weights[bot.difficulty];const hit=Math.random()<skill;return{action:bot.difficulty==='Extreme'&&Math.random()<.35?'shield':hit?'shoot':'dodge',target:Math.floor(Math.random()*Math.max(1,targetCount)),damage:hit?1:0,accuracy:Math.round(skill*100)}}export function memeCombo(bot:Bot){const photos=['Dramatic pigeon','CEO cat','Confused astronaut','Tiny horse','Laser grandma'],audio=['airhorn','sad trombone','dramatic gasp'],subs=['This was not in the plan','When the group chat gets quiet','Absolutely built different'];if(bot.difficulty==='Extreme')return{photo:'CEO cat',audio:'dramatic gasp',subtitle:'When the group chat gets quiet',synergy:97};if(bot.difficulty==='Hard')return{photo:photos[Math.floor(Math.random()*3)],audio:audio[2],subtitle:subs[Math.floor(Math.random()*2)],synergy:78};return{photo:photos[Math.floor(Math.random()*photos.length)],audio:audio[Math.floor(Math.random()*audio.length)],subtitle:subs[Math.floor(Math.random()*subs.length)],synergy:Math.round(25+Math.random()*45)}}export function rewardForPlacement(place:number,total:number){if(place===1)return 100;if(place===total)return 25;return Math.round(100-(place-1)*(75/Math.max(1,total-1))) }